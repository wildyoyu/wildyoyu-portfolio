---
const name  = "wildyoyu";
const about = "QUÉ PASA YOYUS!!!! Nature lover: wildlife, macro y espacio exterior. Buscando el detalle y la historia en cada escena.";
const photo = "/me.JPG";
---
<section class="section">
  <div class="container intro">
    <figure class="intro-avatar">
      <div class="avatar surface">
        <img src={photo} alt={`Retrato de ${name}`} width="800" height="1000" loading="eager" decoding="async" />
      </div>
      <figcaption class="sr">Retrato de {name}</figcaption>
    </figure>

    <div class="intro-copy">
      <h1 class="h1" style="margin:0 0 .35rem 0;">{name}</h1>
      <p class="lead" style="margin:0 0 1rem 0;">{about}</p>

      <div class="intro-box surface">
        <p class="lead" style="margin:0;">En este espacio comparto mis momentos. Si quieres ver o saber más acerca de mí, ¡échale un vistazo!</p>

        <div class="btn-row" style="margin-top:.8rem;">
          <a href="/gallery" class="btn btn-primary">
            Ver toda la galería
            <svg class="i" viewBox="0 0 24 24" fill="none" aria-hidden="true">
              <path d="M5 12h12M13 6l6 6-6 6" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </a>
          <a href="https://instagram.com/wildyoyu" target="_blank" rel="noopener noreferrer" class="btn btn-outline btn-sm" aria-label="Instagram">
            <svg class="i" viewBox="0 0 24 24" fill="none" aria-hidden="true">
              <rect x="3" y="3" width="18" height="18" rx="5" stroke="currentColor" stroke-width="1.6"/>
              <circle cx="12" cy="12" r="3.6" stroke="currentColor" stroke-width="1.6"/>
              <circle cx="17.3" cy="6.7" r="1.1" fill="currentColor"/>
            </svg>
            Instagram
          </a>
          <a href="https://www.tiktok.com/@wildyoyu_" target="_blank" rel="noopener noreferrer" class="btn btn-outline btn-sm" aria-label="TikTok">
            <svg class="i" viewBox="0 0 24 24" aria-hidden="true">
              <path d="M14.5 3c.5 2.3 1.9 3.9 4.3 4.3v3.1c-1.7-.1-3.2-.7-4.3-1.6v5.7a6.6 6.6 0 11-6.6-6.6c.6 0 1.2.1 1.8.3v3.2a3.5 3.5 0 00-1.8-.5 3.4 3.4 0 103.4 3.4V3h3.2Z" fill="currentColor"/>
            </svg>
            TikTok
          </a>
        </div>
      </div>
    </div>
  </div>

  <style>
    .intro{
      display:grid;
      grid-template-columns: minmax(220px, 0.85fr) 1.15fr;
      gap: clamp(18px,3vw,36px);
      align-items:center;
    }
    @media (max-width: 900px){ .intro{ grid-template-columns: 1fr; } }

    .intro-avatar{ justify-self:center; margin:0; }
    .avatar{
      width: min(240px, 24vw);
      aspect-ratio: 1 / 1;
      border-radius: 9999px;
      overflow: hidden;
    }
    .avatar img{ width:100%; height:100%; object-fit:cover; object-position:center; display:block; }

    .intro-box{
      margin-top:.6rem; padding: clamp(14px,1.6vw,18px);
      border-radius: var(--radius);
    }

    .btn-row{ display:flex; flex-wrap:wrap; gap:10px; align-items:center; }
    @media (max-width: 600px){ .btn-row{ justify-content:flex-start; } }

    .intro-copy .h1{ margin-bottom:.35rem; }
    .intro-copy::after{
      content:""; display:block; width: 64px; height:1px; margin:.35rem 0 1rem 0;
      background: var(--stroke);
      border-radius: 2px;
    }

    .sr{ position:absolute; left:-9999px; }
  </style>
</section>
